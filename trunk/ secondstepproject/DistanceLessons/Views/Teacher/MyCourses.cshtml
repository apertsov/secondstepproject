@model IEnumerable<DistanceLessons.Models.Cours>

@{
    ViewBag.Title = "Мої предмети";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@*<br/>

@{
    var grid = new WebGrid(Model, rowsPerPage: 10, defaultSort: "Title");
   @grid.GetHtml(
        tableStyle: "center",
        headerStyle: "center",
        alternatingRowStyle: "center",
        htmlAttributes: "cellspacing=10",
        columns: grid.Columns(
            grid.Column("Title", "Назва предмету", format: (item) => new HtmlString(Html.ActionLink((string)item.Title, "Course", new { id = item.CourseId }, new {color="blue" }).ToString())),
            grid.Column("Category.Category1", "Категорія", format: @<i>@item.Category.Category1</i>)
            ));
                          }
                          *@
     <table class="center" cellspacing="10">
<tr>
        <td class="error" colspan="2" align="center" style="border-top: 1px solid silver; padding-top: 5px;"></td>
    </tr>
    <tr>
        <td>
           <b> @Html.DisplayNameFor(model => model.Title)</b>
        </td>
        
        <td>
           <b> @Html.DisplayNameFor(model => model.Category.Category1)</b>
        </td>
        

    </tr>
    <tr>
        <td class="error" colspan="2" align="center" style="border-top: 1px solid silver; padding-top: 10px;"></td>
    </tr>
@foreach (var item in Model)
{
    <tr>
        <td>
          @Html.ActionLink(item.Title, "Course", new { id = item.CourseId })
          
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Category.Category1)
        </td>


    </tr>
}

 <tr>
        <td class="error" colspan="2" align="center" style="border-top: 1px solid silver; padding-top: 20px;"></td>
    </tr>
</table>
