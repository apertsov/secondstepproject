@model List<DistanceLessons.Models.Answer>

 @for (int j = 0; j < Model.Count; j++)
 {
        @Html.CheckBoxFor(model => model[j].Valid, new { disabled="true" })
        @Model[j].Answer1<br />
 }
